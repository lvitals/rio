version: '3.8'

services:
  app:
    build:
      context: ../../
      dockerfile: samples/rio_openresty_mvc/Dockerfile
    ports:
      - "8080:80"
    volumes:
      - .:/app
    environment:
      - RIO_ENV=development
    restart: unless-stopped
